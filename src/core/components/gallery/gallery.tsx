import { CharacterStructure } from "../../models/character";

export function Gallery() {
  // const { state } = useContext(PhotosContext);

  return (
    <>
      {/* <main className="gallery">
        {state?.photos.map((item: CharacterStructure) => (
          <div></div>
          // <Card info={item} key={item.id}></Card>
        ))}
      </main> */}
    </>
  );
}
